import React from 'react'

/**
 * Branch execution timeline placeholder.
 *
 * Backend already records per-branch timings; this component can be
 * wired into the execution detail view later. For now it renders
 * nothing so it is safe and does not affect existing flows.
 */
export default function BranchTimeline() {
  return null
}


