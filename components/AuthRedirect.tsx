'use client'

// Auth is now opt-in per action via useAuthGuard().requireAuth()
// Pages are publicly accessible; protected actions show a sign-in modal.
export default function AuthRedirect() {
  return null
}
